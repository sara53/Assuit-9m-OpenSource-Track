# component Communications

    -- one way binding
        -- interpolation
        -- attribute binding

== Event Binding
-- two way binding
-- normal way
-- forms Module

# directives

    - Structural Directives
        @for @If @switch -- (angular 17) --- (angular 19) -- control flow
        (*ngFor -- *ngIf -- *ngSwtich)

    -- attribute directives
        [ngClass] [ngStyle] [class.className] [style.propName]

    -- component Directive

== simple Demo ()
